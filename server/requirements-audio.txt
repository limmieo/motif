# Optional local audio-to-MIDI feature.
# Basic Pitch currently supports Python 3.7-3.11.
basic-pitch
librosa>=0.10,<0.12
yt-dlp[default]>=2026.6.9
torch
torchaudio
piano-transcription-inference
# Symbolic key, chord, and voice-leading analysis. Chordino itself is a
# native Vamp plugin used through sonic-annotator when installed; the app
# falls back to librosa chroma automatically when it is unavailable.
music21>=9,<10
more-itertools>=8.6
webcolors>=1.5
# Source separation (vocals / drums / bass / other) before transcription.
demucs
